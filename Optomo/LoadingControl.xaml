<!-- LoadingControl.xaml -->
<UserControl x:Class="Optomo.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="White" Opacity="0.8">
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Ellipse Width="50" Height="50" Stroke="Gray" StrokeThickness="5">
                <Ellipse.RenderTransform>
                    <RotateTransform x:Name="RotateTransform" CenterX="25" CenterY="25"/>
                </Ellipse.RenderTransform>
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                        <BeginStoryboard>
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation 
                                    Storyboard.TargetName="RotateTransform"
                                    Storyboard.TargetProperty="Angle"
                                    From="0" To="360" Duration="0:0:1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
            </Ellipse>
            <TextBlock Text="Yükleniyor..." HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10,0,0" FontSize="20"/>
        </Grid>
    </Grid>
</UserControl>
